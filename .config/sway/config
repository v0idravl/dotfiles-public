# Minimal sway config                   ~/.config/sway/config
#
# Mod = Super (the Windows key). Reload after edits with  Mod+Shift+c.
# Kept deliberately small — learn this set first, grow it only as needed.
# NOTE: sway has NO inline comments. '#' is only a comment at line start.

set $mod Mod4
set $term alacritty
set $menu fuzzel

### Appearance ###
# Wallpaper + focused-border colors live in ~/.config/sway/theme.conf, which
# theme-set rewrites on every switch (dragon: the set-wallpaper PNG, purple
# focus border; redlight: solid black bg, dim red focus border). Reload after a
# switch is automatic (theme-set runs swaymsg reload).
include ~/.config/sway/theme.conf
# Terminess Nerd Font — one family for the whole desktop (package manager or
# nerdfonts.com): digital face, full-size icon glyphs
font pango:Terminess Nerd Font 10
# cursor theme (Adwaita ships cursors on Debian)
seat seat0 xcursor_theme Adwaita 24
gaps inner 6
default_border pixel 2

### Outputs ###
# Pinned layout so monitor hotplug is deterministic instead of sway-default
# roulette. Adjust names/positions to your hardware (swaymsg -t get_outputs).
# eDP-1 = laptop panel, HDMI-A-2 = external monitor.
# No refresh pin, so sway takes the best mode each panel offers. To force a
# mode: resolution 1920x1080@59.940Hz on the output's line.
output eDP-1    resolution 1920x1080 position 0    0
output HDMI-A-2 resolution 1920x1080 position 1920 0
# theme-set owns `output * bg ...` lines; those live in theme.conf, included
# above, and apply per-output on top of this layout.

### Input (laptop) ###
input "type:touchpad" {
    tap enabled
    natural_scroll enabled
    dwt enabled
}
input "type:keyboard" {
    xkb_layout us
}

### Autostart ###
# copy-on-select everywhere: mirror the primary selection (mouse highlight)
# into the clipboard. Terminal/tmux path stays OSC 52 — same clipboard sink.
exec wl-paste --primary --watch wl-copy
# status bar — via waybar-run, a flock-guarded supervisor (~/.local/bin):
# exactly one waybar no matter how many reloads/manual starts, restarted
# after any crash, logging to ~/.local/state/waybar.log. theme-set's
# SIGUSR2 live-reload still works (it signals the bar, not the supervisor).
exec ~/.local/bin/waybar-run
# desktop notifications
exec mako
# GUI auth prompts (pkexec/polkit)
exec lxqt-policykit-agent
# idle: lock at 5m, screen off at 10m, lock before sleep
# swaylock colors come from ~/.config/swaylock/config (theme-set generated)
exec swayidle -w \
    timeout 300 'swaylock -f' \
    timeout 600 'swaymsg "output * dpms off"' \
    resume      'swaymsg "output * dpms on"' \
    before-sleep 'swaylock -f'

### Core keys ###
bindsym $mod+Return exec $term
bindsym $mod+d      exec $menu
bindsym $mod+q      kill
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exec swaynag -t warning -m 'Exit sway?' -B 'Yes' 'swaymsg exit'

# focus (vim keys + arrows)
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move the focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9

# layout
bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+f fullscreen
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle

# resize: enter mode, hjkl to size, Enter/Escape to leave
mode "resize" {
    bindsym h resize shrink width 10px
    bindsym j resize grow height 10px
    bindsym k resize shrink height 10px
    bindsym l resize grow width 10px
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

### Media / brightness / screenshot ###
# volume via ALSA directly; amixer acts on the default card's Master
# (if you run pipewire/pulse instead, swap these for wpctl/pactl)
bindsym XF86AudioRaiseVolume exec amixer -q sset Master 5%+
bindsym XF86AudioLowerVolume exec amixer -q sset Master 5%-
bindsym XF86AudioMute        exec amixer -q sset Master toggle
bindsym XF86MonBrightnessUp   exec brightnessctl set 5%+
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-
# region screenshot -> clipboard (ephemeral — the next copy overwrites it)
bindsym Print        exec grim -g "$(slurp)" - | wl-copy
bindsym $mod+Shift+s exec grim -g "$(slurp)" - | wl-copy
# full-screen screenshot -> timestamped PNG in ~/Pictures
bindsym $mod+Print exec grim ~/Pictures/screenshot-$(date +%Y%m%d-%H%M%S).png

### Lock now ###
bindsym $mod+Escape exec swaylock -f

### Floating rules ###
for_window [app_id="nm-connection-editor"] floating enable

include /etc/sway/config.d/*
