# swaylock — non-color settings only. All *color= keys are GENERATED onto
# the end of ~/.config/swaylock/config by theme-set (dragon/redlight);
# swaylock reads its config fresh on every lock.
# validated against swaylock 1.8.2 (man swaylock).

font=Terminess Nerd Font
font-size=24
indicator-radius=100
indicator-thickness=8
